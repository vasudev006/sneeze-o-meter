:root {
    --green: #07f57b;
    --pink: #bf1578;
    --black: #0b0b0b;
    --cream: #f4f0d8;
}

* {
    box-sizing: border-box;
}

html,
body {
    min-height: 100%;
    margin: 0;
}

body {
    background: var(--green);
    color: var(--black);
    font-family: Arial, Helvetica, sans-serif;
    font-weight: 900;
    overflow-x: hidden;
    padding: 34px 22px 60px;
    position: relative;
}

/* BACKGROUND GRAPHICS */

body::before {
    content: "";
    position: fixed;

    width: 38vw;
    height: 38vw;

    max-width: 520px;
    max-height: 520px;

    left: -14vw;
    top: 12vh;

    background: var(--pink);

    border: 12px solid var(--black);

    transform: rotate(-17deg);

    z-index: 0;

    pointer-events: none;
}

body::after {
    content: "";

    position: fixed;

    width: 260px;
    height: 260px;

    right: -95px;
    bottom: -70px;

    border-radius: 50%;

    background: var(--pink);

    border: 10px solid var(--black);

    z-index: 0;

    pointer-events: none;
}


/* MAIN */

.container {
    width: min(1100px, 100%);
    margin: auto;

    position: relative;
    z-index: 2;
}


/* EXTRA GRAPHIC ELEMENTS */

.container::before {
    content: "";

    position: fixed;

    width: 85px;
    height: 85px;

    right: 9vw;
    top: 90px;

    background: var(--black);

    transform: rotate(22deg);

    z-index: -1;

    box-shadow:
        22px 22px 0 var(--pink);
}

.container::after {
    content: "✳";

    position: fixed;

    left: 7vw;
    bottom: 7vh;

    font-size: 76px;

    line-height: 1;

    color: var(--black);

    transform: rotate(-12deg);

    z-index: -1;
}


/* SCREENS */

.screen {
    display: none;

    min-height: calc(100vh - 94px);

    position: relative;
}

.screen.active {
    display: flex;

    flex-direction: column;

    align-items: center;

    justify-content: center;

    animation:
        enter 0.45s cubic-bezier(.16, 1, .3, 1);
}


/* HEADINGS */

.main-title,
.section-title {
    font-family: "Times New Roman", Times, serif;

    font-weight: 900;

    color: var(--black);

    line-height: .82;

    letter-spacing: -5px;

    margin: 0;

    text-transform: uppercase;

    text-align: center;
}

.main-title {
    font-size: clamp(58px, 10vw, 128px);

    max-width: 1050px;
}

.section-title {
    font-size: clamp(45px, 8vw, 92px);

    max-width: 900px;
}


/* HERO */

.hero {
    width: 100%;

    display: flex;

    flex-direction: column;

    align-items: center;

    position: relative;
}


/* SMALL GRAPHICS */

.hero::before {
    content: "01";

    position: absolute;

    left: 0;
    top: -25px;

    background: var(--black);

    color: var(--green);

    padding: 10px 15px;

    font-size: 13px;

    transform: rotate(-6deg);
}

.hero::after {
    content: "SNEEZE / TEST / CERTIFY";

    position: absolute;

    right: 0;
    top: 25px;

    font-size: 10px;

    letter-spacing: 2px;

    writing-mode: vertical-rl;

    transform: rotate(7deg);
}


/* HIDE OLD TAGLINE */

.tagline {
    display: none;
}


/* GIF CONTAINERS */

.home-gif,
.listening-gif,
.analysis-gif,
.personality-gif {

    background: var(--black);

    border: 6px solid var(--black);

    padding: 8px;

    box-shadow:
        14px 14px 0 var(--pink);

    position: relative;

    overflow: hidden;
}


.home-gif {
    width: min(400px, 80vw);

    margin: 40px auto 34px;

    transform: rotate(-2deg);
}


.listening-gif {
    width: min(360px, 78vw);

    margin: 30px auto 24px;

    transform: rotate(2deg);
}


.analysis-gif {
    width: min(300px, 72vw);

    margin: 0 auto 28px;

    transform: rotate(-3deg);
}


/* GIF LABEL */

.home-gif::before,
.listening-gif::before,
.analysis-gif::before {

    content: "GIF";

    position: absolute;

    top: 7px;
    left: 7px;

    background: var(--green);

    color: var(--black);

    padding: 4px 7px;

    font-size: 9px;

    z-index: 3;
}


/* BUTTONS */

button {
    font-family: Arial, Helvetica, sans-serif;

    font-weight: 900;

    cursor: pointer;
}

.primary-button {

    background: var(--black);

    color: var(--green);

    border: 4px solid var(--black);

    padding: 16px 28px;

    font-size: 14px;

    letter-spacing: 1px;

    box-shadow:
        9px 9px 0 var(--pink);

    transition: .16s ease;

    transform: rotate(-1deg);
}


.primary-button:hover {

    transform:
        translate(4px, 4px)
        rotate(1deg);

    box-shadow:
        4px 4px 0 var(--pink);
}


.primary-button:active {

    transform:
        translate(8px, 8px);

    box-shadow: none;
}


/* LISTENING */

#listeningScreen {
    justify-content: center;
}


/* STATUS TEXT */

.status-text {
    display: none;
}


/* WAITING TEXT */

.waiting-text {

    background: var(--pink);

    color: var(--cream);

    border: 4px solid var(--black);

    padding: 11px 18px;

    font-size: 13px;

    letter-spacing: 1px;

    transform: rotate(-2deg);

    box-shadow:
        7px 7px 0 var(--black);
}


/* DETECTED BOX */

.detected-box {

    display: none;

    margin: 28px auto 0;

    background: var(--black);

    color: var(--green);

    border: 4px solid var(--black);

    padding: 22px;

    box-shadow:
        10px 10px 0 var(--pink);
}


.detected-box.show {

    display: block;

    animation:
        pop .35s ease;
}


.detected-icon {
    font-size: 48px;
}


.detected-title {

    font-family: "Times New Roman", serif;

    font-size: 34px;

    line-height: .9;

    margin: 8px 0 20px;

    color: var(--green);
}


/* ANALYSIS */

.stats-card {

    width: min(680px, 94vw);

    background: var(--black);

    color: var(--cream);

    border: 5px solid var(--black);

    box-shadow:
        12px 12px 0 var(--pink);

    padding: 8px 22px;

    transform: rotate(1deg);
}


.stat {

    display: grid;

    grid-template-columns:
        1fr auto auto;

    gap: 14px;

    align-items: center;

    padding: 18px 2px;

    border-bottom:
        2px solid rgba(244, 240, 216, .2);
}


.stat:last-child {
    border-bottom: 0;
}


.stat-label {

    font-size: 11px;

    letter-spacing: 1px;

    text-align: left;

    color: #aaa;
}


.stat-value {

    font-size: 23px;

    color: var(--green);

    white-space: nowrap;
}


.stat-unit {

    font-size: 11px;

    color: var(--pink);

    white-space: nowrap;
}


.stat-small {

    font-size: 16px;

    color: var(--green);

    white-space: nowrap;
}


/* PERSONALITY */

.personality-gif {

    width: min(330px, 76vw);

    margin: 25px auto 18px;
}


.personality-gif-item {
    display: none;
}


.personality-gif-item.selected {
    display: block;
}


.personality-card {

    background: var(--black);

    color: var(--cream);

    border: 4px solid var(--black);

    width: min(720px, 94vw);

    padding: 28px 24px;

    box-shadow:
        10px 10px 0 var(--pink);

    transform: rotate(-1deg);
}


.personality {

    font-family: "Times New Roman", serif;

    font-size: clamp(32px, 6vw, 58px);

    line-height: .9;

    color: var(--green);
}


/* HIDE DESCRIPTION */

.personality-description {
    display: none;
}


/* AWARD BUTTON */

.award-button {
    margin-top: 32px;
}


/* CERTIFICATE */

.certificate {

    position: relative;

    width: min(760px, 94vw);

    padding: 50px 30px;

    background: var(--cream);

    color: var(--black);

    border: 6px solid var(--black);

    box-shadow:
        14px 14px 0 var(--pink);

    transform: rotate(-1deg);
}


.certificate::before {

    content: "";

    position: absolute;

    inset: 13px;

    border:
        3px dashed var(--pink);

    pointer-events: none;
}


.certificate-small,
.certificate-label,
.certificate-footer {
    position: relative;
}


.certificate-small {

    font-size: 10px;

    letter-spacing: 3px;
}


.certificate-title {

    position: relative;

    margin: 16px 0;

    font-family: "Times New Roman", serif;

    font-size: clamp(42px, 7vw, 68px);

    line-height: .82;

    color: var(--pink);

    letter-spacing: -4px;
}


.certificate-line {

    position: relative;

    width: 90px;

    height: 6px;

    background: var(--black);

    margin: 20px auto;
}


.certificate-label {

    font-size: 10px;

    letter-spacing: 2px;

    margin: 18px 0 8px;
}


.certificate-personality {

    position: relative;

    font-family: "Times New Roman", serif;

    font-size: 36px;

    font-weight: 900;
}


.award {

    position: relative;

    font-family: "Times New Roman", serif;

    font-size: 32px;

    color: var(--pink);

    line-height: 1;
}


.certificate-seal {

    position: relative;

    font-size: 52px;

    margin-top: 18px;
}


.certificate-footer {

    font-size: 9px;

    letter-spacing: 1px;

    margin-top: 20px;
}


/* REMOVE UNNECESSARY LISTENING TEXT */

#listeningScreen .status-text,
#listeningScreen .detected-box p {
    display: none;
}


/* ANIMATIONS */

@keyframes enter {

    from {

        opacity: 0;

        transform:
            translateY(22px)
            rotate(.5deg);
    }

    to {

        opacity: 1;

        transform:
            translateY(0)
            rotate(0);
    }
}


@keyframes pop {

    from {

        opacity: 0;

        transform:
            scale(.8)
            rotate(-3deg);
    }

    to {

        opacity: 1;

        transform:
            scale(1)
            rotate(0);
    }
}


/* MOBILE */

@media (max-width: 650px) {

    body {
        padding:
            24px
            15px
            40px;
    }

    .main-title,
    .section-title {
        letter-spacing: -3px;
    }

    .hero::after {
        display: none;
    }

    .hero::before {
        left: 3px;
        top: -34px;
    }

    .stat-label {
        font-size: 9px;
    }

    .stat-value {
        font-size: 18px;
    }

    .stat-small {
        font-size: 12px;
    }
}